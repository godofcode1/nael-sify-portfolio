# Nael Sify Portfolio

Flask portfolio website with project cards linking to standalone website projects.

## Local development

```bash
pip install -r requirements.txt
python app.py
```

## Render deployment

- Build command: `pip install -r requirements.txt`
- Start command: `gunicorn app:app`
